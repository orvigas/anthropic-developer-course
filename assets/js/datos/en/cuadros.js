/* Comparison charts by module
   Anthropic Certification · Developer

   Extracted from the tables in markdown/ except the M1 ones, which that
   module does not carry and were written from its prose. The order must
   match the Spanish bank: the page links them by position. */

"use strict";

const CUADROS = [
    // --------- M1 ---------
    {m:'M1', titulo:'The four tiers of the Claude family',
     cols:['Tier', 'What it is built for', 'When to choose it', 'How you change tier'],
     filas:[
        ['Haiku', 'Speed and cost efficiency on tasks that fit its capability envelope.', 'Latency or cost dominates and the task fits that envelope.', 'Move down to Haiku only when an eval shows the quality drop is acceptable.'],
        ['Sonnet', 'The balanced default across cost, latency and capability.', 'It is the practical starting point for most production workloads.', 'Start here; move up or down only with an eval behind the decision.'],
        ['Opus', 'Demanding work above the Sonnet envelope.', 'An eval shows Sonnet missing your quality bar.', 'Move up a tier only when the eval justifies it, not on intuition.'],
        ['Fable', 'The most capable tier: the most demanding reasoning, coding and agentic work.', 'Maximum intelligence is the priority over cost.', 'Confirm identifiers against platform.claude.com/docs at build time: the family is evolving.'],
     ]},
    {m:'M1', titulo:'The two context window failures',
     cols:['Situation', 'When it is caught', 'What the API returns', 'What your application must do'],
     filas:[
        ['Input is already larger than the window', 'Before generation begins.', 'A validation error: the whole request is rejected.', 'Trim or summarize history before each call.'],
        ['Input fits, but the ceiling is reached mid-generation', 'During generation.', 'The output produced so far, with a <code class="inline">model_context_window_exceeded</code> stop reason.', 'Read the stop reason; do not treat the response as complete.'],
        ['Neither shows up in development', 'Not caught: test inputs are short.', 'Nothing. The window rarely fills.', 'Measure the budget against production inputs, not test ones.'],
     ]},
    {m:'M1', titulo:'The three prompting modes',
     cols:['Mode', 'What the prompt includes', 'When to use it', 'What it costs'],
     filas:[
        ['Zero-shot', 'The instruction and no examples.', 'The task is simple and the output shape is obvious.', 'Nothing extra. It is the starting point.'],
        ['One-shot', 'One example of the input paired with the desired output.', 'The output has a structure a description keeps failing to pin down.', 'The tokens of that example, on every call.'],
        ['Multi-shot (few-shot)', 'Several such examples.', 'Casing or edge cases remain that one example does not cover.', 'The tokens of every example, on every call, against the context budget.'],
     ]},
    {m:'M1', titulo:'The four API access patterns',
     cols:['Pattern', 'How the response arrives', 'When to use it', 'What you need to know'],
     filas:[
        ['Synchronous', 'Complete, in one piece, after the wait.', 'Short responses and backend work where nobody is waiting on screen.', 'It is the simplest pattern.'],
        ['Streaming (SSE)', 'In pieces, while the model generates it.', 'A user is watching, or the response is long.', 'Your code reassembles the pieces and must recover if the stream breaks.'],
        ['SDK async client', 'In real time, without tying up your application thread.', 'You need concurrency without blocking.', 'In Python it is <code class="inline">AsyncAnthropic</code>; in TypeScript the standard client is already promise-based and there is no separate class.'],
        ['Message Batches API', 'Deferred: you receive an identifier and poll for completion.', 'Bulk offline work where cost matters more than turnaround time.', 'Up to 24 hours of latency in exchange for a lower per-token cost.'],
     ]},
    {m:'M1', titulo:'Model choice and reasoning mode compose',
     cols:['Combination', 'How it behaves', 'When it fits'],
     filas:[
        ['Capable model, reasoning off', 'Fast and direct.', 'The task is demanding on capability but not on reasoning steps.'],
        ['Smaller model, reasoning on', 'Spends more tokens to think.', 'The problem is multi-step even though it does not need the largest model.'],
        ['Capable model, higher effort', 'The costliest of the three.', 'The most demanding tasks. Reasoning is wasted on lookups and classification.'],
     ]},
    // --------- M2 ---------
    {m:'M2', titulo:'Diagnosis: what the prompt is missing',
     cols:['What you observed', 'What the prompt is missing', 'Why this technique is the fix'],
     filas:[
        ['The result comes back in the wrong shape: a sentence where you expected a label, prose where you expected JSON.', 'An output constraint. The prompt never specified the form, field names, or stopping point of the response.', 'An output constraint controls the form of the response independent of its content. Without one, Claude returns plausible text that the downstream parser was not built to accept.'],
        ['The content is off: scope drifts, tone shifts, or Claude answers a wider question than you asked, and it gets worse deeper into the conversation.', 'A system prompt, or a more specific one. The behavioral contract was too vague to hold across turns.', 'The system prompt sets the rules that apply to every response regardless of the user turn. When it is underspecified, there is nothing holding role, scope, and format steady as the conversation runs on.'],
        ['The task is right, but the structure is invented: Claude understood what to do and produced output in a shape you never asked for.', 'Few-shot examples. Claude cannot infer an exact structure from a description alone.', 'Few-shot examples show the pattern rather than describe it. One correct input-output pair gives Claude the exact shape to match, which a written instruction often fails to pin down.'],
        ['Output is clean on the inputs you tested but breaks on a variant: an edge case, an unusual field, an input you did not anticipate.', 'A constraint covering the variant. The prompt handles the happy path and has no rule for the case the parser breaks on.', 'The prompt was validated against a narrow set of inputs. Naming the variant in the constraint, or adding an example that covers it, closes the gap the test inputs never exposed.'],
     ]},
    {m:'M2', titulo:'The six refinement passes',
     cols:['Pass', 'What was added', 'Output behavior'],
     filas:[
        ['1', '"Classify this ticket as billing, technical, or escalation."', 'Returns full sentences: "This appears to be a billing issue." Parser breaks.'],
        ['2', 'Added "Be concise." and "Use only the category name."', 'Returns "Billing" capitalized sometimes, "billing" lowercase other times. Router breaks on case mismatch.'],
        ['3', 'Added three paragraphs describing each category in detail.', 'Output correct on simple tickets. For ambiguous tickets, returns \'billing/technical\' instead of a single label. Parser breaks on the slash.'],
        ['4', 'Added "Never return two categories." and "If ambiguous, choose the most likely one."', 'Works on 80% of tickets. Fails on tickets that could reasonably fit two categories (e.g., \'I was charged but the feature also stopped working\'). Here it returns a full explanation instead of a label.'],
        ['5', 'Added two more paragraphs about edge cases and a reminder to be precise.', 'The verbose prompt is now producing verbose output, over 2,000 characters per call, as long, unfocused prompts tend to produce long, unfocused outputs. The model calibrates response length and style to match the input. Latency has increased significantly due to output length, but accuracy has not improved.'],
        ['6', 'Replaced all instructions with a JSON schema and two few-shot examples showing exact input/output pairs', 'Returns {"category": "billing"} on every ticket. Parser works. Latency drops. Accuracy on ambiguous tickets matches Pass 4.'],
     ]},
    {m:'M2', titulo:'When to turn on extended thinking',
     cols:['Task shape', 'Extended thinking call', 'Reason'],
     filas:[
        ['Multi-step reasoning where the model has to hold several constraints at once: a math derivation, a multi-hop logic problem, planning a sequence of dependent actions.', 'Enable it, with the effort level matched to the depth of the problem.', 'The reasoning pass is where the model works through dependencies it would otherwise skip.'],
        ['Mechanical or lookup tasks: classification, format conversion, extracting a field, short factual answers.', 'Leave it off.', 'Extended thinking will not improve the answer, and you will be paying more tokens for something you didn’t need. A bare prompt with an output constraint is the right tool.'],
        ['Agentic loops where the model plans across several tool calls.', 'Enable it and budget for the planning step rather than per call.', 'Reasoning before a plan reduces wrong-tool selection downstream. Note the carry-back rule below, which applies in every tool-use loop.'],
     ]},
    {m:'M2', titulo:'The four message block types',
     cols:['Block type', 'Role', 'Contains', 'Critical rule'],
     filas:[
        ['text block', 'Assistant/Claude', 'Claude’s prose output', 'Claude may return a text block alongside a tool_use block in the same turn. When it does, your code must preserve the full content array, including the text block, when appending that turn to conversation history. Dropping the text block corrupts the context Claude relies on for follow-up turns.'],
        ['tool_use block', 'Assistant/Claude', 'The tool name, a unique ID, and the input arguments Claude wants passed to your function', 'Every tool_use block must be answered by a tool_result block in the immediately following user turn. The tool_result must carry the same ID. Without that pairing, the API rejects the next request.'],
        ['tool_result block', 'User', 'Matching tool_use ID, the result content, and an optional is_error flag set to true when the tool call fails', 'The tool_use_id value must match the original tool_use block exactly. Claude uses this ID to connect each result back to the call that produced it, which matters when a single assistant turn issues multiple tool calls and the results arrive in a different order.'],
        ['thinking block', 'Assistant (extended thinking only)/Claude', 'Claude’s internal reasoning, visible only when extended thinking is enabled', 'The block must be passed back to the API unchanged in subsequent turns. The signature verifies the reasoning hasn’t been modified, so any edit or summary breaks the signature and the API rejects the message. Redacted thinking blocks follow the same rule: pass them back as received, even though the content is encrypted and not human-readable.'],
     ]},
    {m:'M2', titulo:'Tool schema design decisions',
     cols:['Decision', 'How to handle it', 'Why it matters'],
     filas:[
        ['Subtask dependency', 'When one tool’s output feeds the next, the calls have to run in sequence because the second call cannot be built until the first result comes back. When the subtasks are independent of each other, you can structure the tool set so Claude issues multiple tool_use blocks in a single turn and your code runs them concurrently.', 'This is the one decision that changes how you design the schema. Current Claude models default to parallel calls when calls are independent. Where a real dependency exists, model it as separate turns so the first result is available before the next call is built. Use disable_parallel_tool_use to force one tool call per turn if needed.'],
        ['Required fields', 'Mark a field as required only when the call doesn’t make sense without it. Place these in the required array of the input schema.', 'Marking everything required forces Claude to fabricate values for fields it has no basis to fill in. The required array is how you tell Claude which inputs are non-negotiable.'],
        ['Optional fields', 'Use optional fields for parameters with sensible defaults or where absence carries meaning. Leave them out of the required array and give them defaults in the function signature.', 'Optional fields let Claude omit information it doesn’t have, instead of guessing. If a field is optional but marked required, every call must invent a value, which can cause bad inputs.'],
        ['Description length', 'Write three to four sentences per tool covering what it does, when Claude should reach for it, and what it returns. Include examples of valid inputs where format matters.', 'If the description is too short, Claude guesses because there isn’t enough signal to distinguish your tool from others. If the description is too long, the trigger conditions get buried under detail Claude doesn’t reference at decision time.'],
        ['Overlapping parameter types', 'When two tools accept the same parameter shape, add disambiguating language to each description that names the domain or trigger the tool is meant for.', 'Claude routes on name plus description, with parameter types as a secondary signal. When signatures are identical, routing collapses to description alone, and similar-sounding descriptions become indistinguishable.'],
     ]},
    {m:'M2', titulo:'The six streaming events',
     cols:['Event', 'What it signals', 'What your handler does'],
     filas:[
        ['message_start', 'A new message is beginning. Carries the message shell with empty content and initial usage.', 'Set up an empty content array to collect blocks in.'],
        ['content_block_start', 'A new content block is opening, with its type (text, tool_use, or thinking) and index.', 'Make a slot at that index for the named block type. A tool_use block opens with its name and id, but no input yet.'],
        ['content_block_delta', 'An incremental piece of one block: a text fragment, a fragment of JSON input for a tool call, or a thinking fragment.', 'Append the fragment to the block at that index. Tool-call inputs arrive as a partial JSON string spread across several deltas, you can\'t parse them until the block closes.'],
        ['content_block_stop', 'The block at this index is complete.', 'Finalize the block. For a tool_use block, this is the first moment the accumulated JSON input is complete enough to parse.'],
        ['message_delta', 'Top-level changes to the message: the stop_reason and final usage counts.', 'Record the stop_reason. It tells you whether the model finished or stopped for some other reason.'],
        ['message_stop', 'The stream is complete.', 'The assembled content array is now the finished message. From here, treat it exactly like a non-streamed response.'],
     ]},
    {m:'M2', titulo:'Four context budget strategies',
     cols:['Strategy', 'What it does', 'When to apply', 'What continuity you lose'],
     filas:[
        ['<strong>Pruning</strong>', 'Lets you jump back to an earlier message and continue from there, removing the conversation that came after.', 'After Claude has gone down an unproductive path or accumulated debugging back-and-forth that won\'t help the next task.', 'The work done after the rewind point is gone. If Claude learned something useful in that stretch, it has to relearn it.'],
        ['<strong>Compaction</strong>(<code class="inline">/compact</code>in Claude Code; server-side compaction in the API, a beta strategy the platform performs for you, with manual summarization as the client-side alternative)', 'Summarizes the conversation history into a condensed version that preserves the key information Claude has learned. The summary costs fewer tokens than the original turns.', 'When the session is approaching the context ceiling but you want to keep working on the same feature with the knowledge Claude has built up.', 'Details can be lost in the summarization. Anything not captured in the summary will not be available to Claude going forward.'],
        ['<strong>Clearing</strong>(<code class="inline">/clear</code>in Claude Code; new session in API)', 'Starts a new conversation with empty context. Nothing from the previous session carries forward.', 'When the next task is completely different from the current one, and previous context would only introduce bias or confusion.', 'All session context is gone. Anything Claude needs to remember across sessions has to be put somewhere persistent, like a CLAUDE.md file.'],
        ['<strong>Subagent Handoffs</strong>', 'Spawns a subagent in its own isolated context window with only the task description and system prompt it needs. The subagent does the work and returns a summary.', 'When a subtask is self-contained enough to delegate, especially exploration work where the journey clutters the main context but the answer is short.', 'Visibility into how the subagent reached its conclusion. The intermediate steps are discarded with the subagent\'s context.'],
     ]},
    {m:'M2', titulo:'Context budget: development versus production',
     cols:['', 'Development', 'Production'],
     filas:[
        ['Context window available', '200k standard, 1M on current Opus and Sonnet', '200k standard, 1M on current Opus and Sonnet'],
        ['Team budget cap', '40k tokens', '40k tokens'],
        ['Avg. tool output', '~800 tokens per call', '~3,200 tokens per call'],
        ['Turns before window fills', 'Sessions completed without reaching the cap', 'Cap reached at turn 8'],
        ['Observed symptom', 'None. Sessions complete cleanly', 'Wrong tool selections and incomplete outputs starting turn 8'],
        ['Root cause identified by', 'Not applicable', 'Token usage audit, two days after deployment'],
        ['Fix', 'Not applicable', 'Prune tool outputs after use, and apply compaction proactively before the cap is reached'],
     ]},
    {m:'M2', titulo:'Managed agents: what you give up and take on',
     cols:['Category', 'What you stop owning', 'What you take on instead'],
     filas:[
        ['Execution & infrastructure', 'The iteration loop, the execution sandbox, the retries inside the loop, and the tool-execution runtime. Anthropic runs all of it server-side.', 'An agent definition managed as a versioned API resource, plus an application layer that sends events and consumes the streamed results.'],
        ['Session duration & state', 'Long-running execution management. Sessions can run for minutes or hours without your process holding the loop open.', 'Server-side session state. Sessions are stateful and stored by Anthropic, and are subject to its data handling policies and constraints (see the constraint note below).'],
        ['Sandbox lifecycle', 'Sandbox provisioning and teardown for tool execution.', 'A dependency on the managed sandbox\'s available tools and its execution model, rather than your own environment.'],
     ]},
    {m:'M2', titulo:'Agent loop wiring checklist',
     cols:['#', 'Item', 'What to verify'],
     filas:[
        ['1', 'Tools registered', 'Every tool the agent may need is in the registration list. No unregistered tools are referenced in the system prompt.'],
        ['2', 'System prompt scoped', 'The system prompt names the task and the available tools. It does not describe tools the agent does not have. It does not omit tools the agent does have that require scoping guidance.'],
        ['3', 'Tool-use loop implemented', 'Your code handles every tool-use block Claude issues and returns a tool-result block for each one before the next assistant turn. All tool-use blocks from a single assistant turn must be resolved together.'],
        ['4', 'HITL insertion point defined', 'At least one point in the loop has a human-in-the-loop check. See the section below for where to insert it.'],
        ['5', 'Exit conditions defined', 'The loop has a clear stopping criterion that does not depend on Claude volunteering to stop.'],
     ]},
    {m:'M2', titulo:'Where to insert the human gate (HITL)',
     cols:['Insertion point', 'What triggers the check', 'Risk level it addresses'],
     filas:[
        ['<strong>Before a destructive tool call</strong>', 'The agent is about to execute a write, delete, or send operation.', '<strong>High:</strong>irreversible actions where a wrong call cannot be undone'],
        ['<strong>After a planning step</strong>', 'The agent has generated a plan and is about to begin executing it.', '<strong>Medium:</strong>incorrect plans that would produce the wrong outcome even if all steps execute correctly'],
        ['<strong>On unexpected output</strong>', 'The tool result contains an error flag, an empty result, or a value outside expected bounds.', '<strong>Variable:</strong>catches failure modes that retry logic alone will not resolve'],
     ]},
    {m:'M2', titulo:'Regulated data constraints',
     cols:['Constraint', 'What it tends to rule out in code', 'What usually survives a code review'],
     filas:[
        ['Attorney-client privilege', 'Calls from a consumer-grade Claude.ai surface that the firm cannot audit end-to-end. Code paths that send privileged document content to any endpoint the firm has not approved for privileged material, regardless of how the prompt or system message is structured.', 'Direct API or SDK calls from inside the firm\'s own application, authenticated via SSO, routed through a firm-approved LLM gateway with full request and response logging. Note that Anthropic\'s native Compliance Conversation content (prompts, responses, and tool call payloads) is not captured by Anthropic by default on direct API traffic, so the organization must implement conversation logging in the application layer and route it to an approved log destination. Tool calls and tool results stay inside the audited path. Confirm the final logging design with your Anthropic account team.'],
        ['HIPAA (PHI handling)', 'Code that sends Protected Health Information to any endpoint or delivery route not covered by a Business Associate Agreement for the specific configuration in use. This includes any logging or retention path your code writes to that has not been scoped under the same BAA.', 'Direct API or SDK calls on a BAA-covered configuration. BAA coverage for Anthropic first-party API access is arranged with Anthropic, which provisions a dedicated HIPAA-enabled organization that enforces feature restrictions on its own end. Confirm the covered configuration with your Anthropic account team. An alternative is a cloud-mediated route via AWS Bedrock or GCP Vertex on the partner\'s existing HIPAA-eligible cloud account.*<strong>Note</strong>*:<em>the BAA does not cover Console, Workbench, beta features, or consumer plans. Not all API features are covered under the BAA, verify the current feature eligibility list in Anthropic\'s Implementation Guide before configuring.</em>'],
        ['GDPR and data residency', 'Delivery routes where the region of model execution cannot be pinned in code, or where the request can be served from a region outside the approved geographic boundary. Defaulting to a global endpoint without specifying region is the common pattern that breaks here.', 'A cloud-mediated route such as Bedrock or Vertex, with the region pinned in the client configuration to a covered jurisdiction. The direct Anthropic API is a separate case; it does not currently provide EU data residency, so partners with EU data residency requirements should route through Bedrock or Vertex rather than calling the API directly.'],
        ['FedRAMP and government', 'Any code path that calls an endpoint not on an authorized cloud environment at the required impact level. This includes development and test paths that hit the commercial endpoint while production hits the authorized one, because credentials and code patterns leak between them.', 'Three authorized routes exist as of publish time. Claude for Government (C4G) carries a direct FedRAMP High authorization held through Palantir Federal Cloud Service – Supporting Services (PFCS-SS). Claude via Amazon Bedrock GovCloud is approved for FedRAMP High and DoD IL4/5 workloads. Claude via Vertex AI Assured Workloads is also FedRAMP authorized. Claude Enterprise on AWS Marketplace is not FedRAMP authorized, so teams requiring FedRAMP compliance must use one of the three routes above. Verify current authorization status at trust.anthropic.com before configuring.'],
        ['Internal data-residency policy', 'Calls from any SDK client configured against a cloud vendor outside the partner\'s approved list, regardless of whether the underlying technical capability would support the workload. Procurement-level constraints rule the code path out before engineering preferences enter the conversation.', 'The delivery route on the partner\'s approved cloud vendor. In code terms, that is whichever SDK client and endpoint configuration their CIO has already cleared. Build against that one rather than switching mid-project because another route looks easier.'],
     ]},
    {m:'M2', titulo:'Memory patterns',
     cols:['Scope', 'What persists', 'Cost', 'When to use', 'What you lose'],
     filas:[
        ['In-context memory', 'State lives in the active conversation and survives turns within a single session.', 'Zero retrieval overhead; inflates token cost as conversation grows', 'Short sessions where all the state the agent needs fits inside the context window and nothing has to carry across restarts.', 'Everything once the session ends. A clear command or a new session wipes the state.'],
        ['External storage', 'State is written to a database and read back at session start or on demand.', 'Each database call adds retrieval latency, and you take on the engineering work of read and write logic.', 'State that has to survive across sessions, move between users, or be shared across multiple agent instances.', 'Nothing on the persistence side. The cost shows up as latency on every call and ongoing implementation complexity.'],
        ['Summarized memory', 'A condensed version of prior conversation is generated and injected at the start of the next session.', 'Lower token cost per session than replaying full history, but the summarization step drops detail that was in the original.', 'Long-running conversational agents where the full history would outgrow the context budget before the conversation is done.', 'Any detail the summarizer did not preserve. The agent only sees what the summarization prompt chose to keep.'],
        ['No persistent memory (stateless)', 'Nothing. Each session is independent.', 'No overhead at all, since there is nothing to retrieve or store.', 'Task-execution agents that finish and close out, or pipelines where every session is fully independent by design.', 'All prior context. If a follow-up depends on something from an earlier session, the agent has no way to reach it.'],
     ]},
    {m:'M2', titulo:'Skill versus CLAUDE.md versus in-context instructions',
     cols:['Pattern', 'When it loads', 'Context cost', 'Best for'],
     filas:[
        ['Skill (SKILL.md)', 'On demand when request matches skill\'s description', 'Low. Only the name and description load at startup; full content loads only on match', 'Task-specific expertise that should not inflate sessions where it is not needed.<em>Examples include domain-specific output formats, specialized review checklists, and workflows that apply to a subset of tasks rather than every interaction.</em>'],
        ['CLAUDE.md', 'Every session, unconditionally', 'Fixed overhead per session regardless of task', 'Always-on project standards that apply to everything.<em>Examples include coding conventions the team has standardized on, output format rules the project requires, and constraints that hold across all tasks in the codebase.</em>'],
        ['In-context instructions', 'Present for every turn within that session', 'Grows with session length; does not survive session end', 'Short sessions where the full history fits within the window and nothing needs to persist.<em>Examples include one-off exploratory work and tasks scoped to a single conversation.</em>'],
     ]},
    {m:'M2', titulo:'Choosing the right API pattern',
     cols:['Use case', 'Right API pattern', 'Why'],
     filas:[
        ['A user uploads a photo and expects an immediate classification', 'Synchronous API', 'Real-time response is required. Batch latency is unacceptable for interactive use.'],
        ['A nightly pipeline classifies 5,000 customer records', 'Message Batches API', 'Latency is not a constraint. Batch cost reduction and asynchronous processing are both valuable.'],
        ['An evaluation run tests a new prompt against 2,000 examples', 'Message Batches API', 'Offline task with no real-time requirement. Batch is the correct pattern.'],
        ['A chatbot generates a reply to a user\'s message', 'Synchronous API', 'User is waiting; batch would introduce unacceptable delay.'],
     ]},
    // --------- M3 ---------
    {m:'M3', titulo:'Where each piece of context lives',
     cols:['Mechanism', 'What it loads', 'When it runs', 'Context cost', 'Belongs here'],
     filas:[
        ['CLAUDE.md', 'Full file contents prepended to context at session start.', 'Every session, unconditionally.', 'Persistent per session. Dilutes with size.', 'Universal project constraints, commands, and framework decisions.'],
        ['Rules file', 'File contents. Scoped via a<code class="inline">paths</code>glob in YAML frontmatter; without<code class="inline">paths</code>, loads like CLAUDE.md.', 'When Claude reads a file matching the rule’s paths patterns. Unscoped rules load at session start.', 'Path-scoped: adds to context only when triggered. Unscoped: same persistent cost as CLAUDE.md.', 'Path-specific guidance that would be noise everywhere else.'],
        ['Hook', 'Run your script at the lifecycle event. No content added to context.', 'At the configured event (PreToolUse, PostToolUse, etc.).', 'Minimal: only the script output if routed back to Claude.', 'Enforced guardrails, automated side effects, audit logging.'],
        ['Subagent', 'Task context only. Isolated from the main session.', 'When dispatched by the main session for a delegated task.', 'Returns a summary, not the full task history.', 'Exploration, investigation, and tasks whose output would otherwise bloat the main context. Also useful for tasks that can be broken down and parallelized.'],
     ]},
    {m:'M3', titulo:'How a skill loads in each runtime',
     cols:['Runtime', 'How the skill loads', 'Where the steps run', 'What you need to know'],
     filas:[
        ['Claude Code', 'Discovered from<code class="inline">.claude/skills</code>on the filesystem. Loads on a description match or when you invoke it by name.', 'In your terminal session, against your local files, under the active permission mode and deny rules.', 'It’s filesystem-based and is governed by the settings layer.'],
        ['Messages API', 'Sent along with the request and run inside the code execution container, not your application’s environment. Requires code-execution and skills beta headers.', 'Inside Anthropic’s code execution container, not on your machine. The skill’s filesystem and tool access are whatever that container provides.', 'A skill that assumes local files or local tools won’t behave the same way here, because it isn’t running where those files are.'],
        ['Agent SDK', 'Loaded by the agent the SDK runs, but whether filesystem settings (CLAUDE.md, skills) load is controlled by the settingSources configuration. Do not rely on a default: always set it explicitly to the sources you intend, and confirm current default behavior against the Agent SDK reference at build time. You set it through the “settingSources” (TypeScript) / “setting_sources” (Python).', 'In the process the SDK runs, which is your environment, once you’ve told it to load filesystem sources.', 'The common surprise: a skill that worked in Claude Code does nothing under the SDK because settingSources was never set, so the skill never loaded.'],
        ['Managed Agents', 'Defined once as an API resource that names the model, system prompt, tools, MCP servers, and skills. Anthropic loads the skill server-side when the agent runs, so there is no filesystem discovery step on your side.', 'Inside a sandbox Anthropic provisions and runs, not your environment. Your application sends user events and reads streamed results back. The skill has access to whatever that managed sandbox provides, not your local files.', 'Currently a public beta that requires the<code class="inline">managed-agents-2026-04-01</code>beta header, and sessions are stored server-side, which means Managed Agents are not currently eligible for Zero Data Retention or HIPAA BAA coverage. Skills are attached when defining the agent resource, not at session time. Update the agent definition to change which skills are available.'],
     ]},
    {m:'M3', titulo:'Skill, command or plugin: what to package',
     cols:['Layer', 'What it is', 'Who it is for', 'When to reach for it'],
     filas:[
        ['Skill', 'A Markdown file in<code class="inline">.claude/skills</code>that loads when its description matches the task or when invoked by name.', 'An individual developer or team using Claude Code interactively.', 'Reach for a skill when a task-specific procedure should stay out of context until it is needed, such as a PR review or a deployment checklist that only loads when the work calls for it.'],
        ['Custom command', 'A named shortcut that runs a defined procedure when you invoke it explicitly.', 'Developers who want a predictable, explicit entry point for high-frequency procedures.', 'Reach for a custom command when the procedure has a clear name and you want to trigger it directly rather than relying on the description to match the task.'],
        ['Plugin', 'A versioned bundle of skills, hooks, subagents, and MCP servers distributed through a marketplace.', 'A team that wants one-step installation of a shared, versioned setup.', 'Reach for a plugin when a working setup currently lives on one machine and needs to be shared, versioned, and kept consistent across a team.'],
     ]},
    {m:'M3', titulo:'MCP configuration by context',
     cols:['Context', 'Transport', 'Scope', 'Config location', 'Secrets handling'],
     filas:[
        ['Personal local tool (runs on your machine only)', 'stdio', 'Local', '<code class="inline">~/.claude.json</code>(per-project entry)', 'Environment variables only. Never in config file.'],
        ['Shared team server (all teammates connect to same service)', 'HTTP', 'Project (<code class="inline">.mcp.json</code>)', '<code class="inline">.mcp.json</code>committed to repo root', 'OAuth or env variables. API keys must never be committed to .mcp.json.'],
        ['Personal experiment (not ready to share)', 'stdio or HTTP', 'Local', 'Personal Claude settings', 'Environment variables only.'],
        ['Organization-wide deployment (admin-managed)', 'HTTP', 'Enterprise', 'Managed settings (admin-controlled)', 'Secrets managed by administrator. Config locked to prevent override.'],
     ]},
    {m:'M3', titulo:'Authentication by service type',
     cols:['Service type', 'Auth method', 'Where secrets live', 'What gets logged', 'Who can lock the config'],
     filas:[
        ['Remote with user identity (SaaS, cloud)', 'OAuth', 'Token issued by OAuth provider and stored by client.', 'PostToolUse hook to audit log.', 'Administrator via enterprise managed settings.'],
        ['Remote with service identity (internal API)', 'API key in environment variable', 'Environment only. Never in committed config.', 'PostToolUse hook to audit log.', 'Administrator via enterprise managed settings.'],
        ['Local (file system, local DB)', 'File-system permissions', 'No credential needed. Deny rules enforce path access.', 'PostToolUse hook to audit log.', 'Deny rules in enterprise managed settings.'],
     ]},
    // --------- M4 ---------
    {m:'M4', titulo:'Choosing the grading method',
     cols:['Task type', 'Grading method', 'What it catches', 'Where it is unreliable'],
     filas:[
        ['Single correct label or value', 'Exact or string match', 'A wrong answer when there is exactly one correct answer, with zero ambiguity and near-zero cost.', 'Fails every valid paraphrase or reordering, so it is wrong for anything open-ended.'],
        ['Structured or code output', 'Code-graded check', 'Invalid JSON, unparseable code, out-of-range numbers, and missing required fields.', 'Says nothing about whether the content is good, only that it is well-formed.'],
        ['Open-ended quality', 'LLM-as-judge', 'Faithfulness, instruction following, completeness, and tone that no code rule expresses.', 'Noisy and costly and produces a confident-looking number that means nothing until it is calibrated.'],
     ]},
    {m:'M4', titulo:'The five test levels',
     cols:['Level', 'What it isolates', 'What it cannot catch'],
     filas:[
        ['Unit', 'One function, such as a parser or tool wrapper, on its own.', 'Anything about how components fit together.'],
        ['Functional', 'One Claude call returning the expected shape for an input.', 'Failures in the system around that single call.'],
        ['Integration', 'The seam where two components hand off, such as retrieval into the model.', 'Whole-flow behavior that only emerges end to end.'],
        ['End-to-end', 'The full flow as a user runs it, input to output.', 'Where exactly the break is, since it sees only the final result.'],
        ['Retrieval choice', 'Fetch a fixed set once for single-fact lookups in a stable corpus.', 'Multi-step questions and changing corpora, which need search across rounds.'],
     ]},
    {m:'M4', titulo:'Error handling by type',
     cols:['Error type', 'Retriable or fail-fast', 'Backoff strategy', 'Fallback behavior'],
     filas:[
        ['Rate limit (429)', 'Retriable', 'Exponential backoff with jitter, honor retry-after, capped attempts.', 'After the cap, raise a clean error or route to a cached or simpler result.'],
        ['Overloaded (529)', 'Retriable', 'Backoff; a 529 reflects Anthropic-side load, so it is not a rate-limit signal.', 'Fail over to a fallback path or return a graceful error if it persists.'],
        ['Bad request (400)', 'Fail fast', 'No retry. The identical request will fail again.', 'Fix or reject the input and surface the error to the caller.'],
        ['Tool result error', 'Depends on the tool', 'Retry only if the underlying cause is transient.', 'Return the error flag to Claude so the model can react, never silence it.'],
        ['Refusal (200, stop_reason: "refusal")', 'Fail fast', 'No retry. The model made a content decision, not a transient error.', 'Raise the refusal to the caller. Log it. Do not silently retry or treat it as valid output.'],
     ]},
    {m:'M4', titulo:'What to instrument and where',
     cols:['Metric', 'Where to instrument it', 'Single-agent versus orchestrator-worker'],
     filas:[
        ['Token cost', 'Per call, aggregated per request and per flow.', 'A single agent incurs a token cost once per step. An orchestrator-worker multiplies token consumption by the number of subagents, roughly a 15x token multiplier in Anthropic\'s reported case. That multiplier applies to both input and output tokens, since each subagent receives its own context and generates its own output.'],
        ['Latency', 'Per call, with traces identifying the slowest step in the workflow.', 'Parallel subagents can reduce wall-clock time on independent work but add coordination latency to plan and compile.'],
        ['Error rate', 'Per call and per dependency.', 'More agents mean potential failure points, each subagent requires the same retry and fallback handling as a single agent.'],
     ]},
    {m:'M4', titulo:'Threats and the control that blocks them',
     cols:['Threat', 'Where it enters', 'The control that blocks it', 'What gets logged'],
     filas:[
        ['Prompt injection', 'Hidden instructions inside fetched pages, documents, or tool results.', 'Treat fetched content as data, plus a hook that refuses actions triggered by untrusted input.', 'The fetched source, the action attempted, and the block.'],
        ['Jailbreak', 'A user prompt crafted to bypass the model\'s safety constraints.', 'Input validation plus a constraint on what the model is allowed to do.', 'The flagged prompt and the refusal.'],
        ['Over-broad access', 'An identity scoped wider than the task needs.', 'Least-privilege identity, secrets in a manager, locked auth configuration.', 'Every privileged action, with the identity that performed it.'],
        ['Sandbox escape', 'A steered agent attempting filesystem or network access outside its permitted boundary, including paths and endpoints no hook or permission rule explicitly covers.', 'OS-level sandboxing: filesystem isolation scoped to the working directory, network isolation scoped to permitted endpoints only. Configured via Claude Code settings; documented on code.claude.com. The control that holds when a hook or permission rule is missing.', 'Every attempted access outside the sandbox boundary, logged with the tool call that triggered it and the path or endpoint that was denied.'],
     ]},
    // --------- M5 ---------
    {m:'M5', titulo:'The three reusable asset types',
     cols:['Asset type', 'What it bundles', 'What correct packaging requires'],
     filas:[
        ['Agent Template', 'The system prompt, the tool schemas, and the loop structure from a working agent.', 'Pull the domain-specific values into configuration with documented defaults, so a new team sets the values rather than editing the loop.'],
        ['MCP Server Package', 'The tools the server exposes, with their inputs and the scope the installing team controls.', 'Document each tool input and let the installing team set the scope, so the server installs into a new environment without code edits.'],
        ['Eval Suite', 'The graded test set and the judge rubric that prove the asset works.', 'Ship the dataset and rubric together so a new team can run them in their own context and confirm the asset still works there. The same eval suite also acts as the gate at deployment. When you promote a new model version to production, run it against a pinned baseline score before the version goes live.'],
     ]},
    {m:'M5', titulo:'Packaging checklist by asset',
     cols:['Asset type', 'What to parameterize', 'What to document', 'What to bundle for audit'],
     filas:[
        ['Agent template', 'Every value that changes per customer: prompts, paths, scopes, credentials by reference, and thresholds.', 'Environment assumptions, expected inputs, handled failure modes, and the eval that defines working.', 'The data touched, the identity acted under, and the log of what the asset did.'],
        ['MCP Server', 'Scopes, credentials by reference, and per-customer paths.', 'Expected inputs per tool, scope boundaries, and handled failure modes.', 'The data touched, the identity acted under, and the log of what the asset did.'],
        ['Eval Suite', 'Thresholds and dataset paths that change per customer or environment.', 'The rubric logic, what the scores mean, and the baseline the asset is pinned to.', 'The data touched, the identity acted under, and the log of what the asset did.'],
     ]},
    {m:'M5', titulo:'Choosing a deployment platform',
     cols:['Platform', 'Identity and data model', 'When to choose it', 'How versioning is pinned'],
     filas:[
        ['First-party Claude API', 'Anthropic identity and terms.', 'The customer has no binding cloud or residency constraint and wants the newest capabilities.', 'Pin the full model ID and keep the prior snapshot.'],
        ['Claude Platform on AWS', 'Anthropic identity and terms, accessed through the customer\'s AWS account; inference is Anthropic-operated outside the AWS boundary. Model lifecycle follows Anthropic\'s deprecation schedule.', 'The customer is on AWS but wants Anthropic model IDs, lifecycle, and feature parity with the first-party API.', 'Pin using the same model ID format as the Claude API (for example, claude-opus-4-8). Lifecycle follows Anthropic\'s schedule. (Confirm at publish time.)'],
        ['Claude in Amazon Bedrock', 'Messages API at /anthropic/v1/messages, broad feature parity with the first-party API; confirm feature-specific requirements against the Bedrock documentation. Data stays inside the customer\'s configured AWS boundary.', 'The customer is on AWS, wants broad feature parity with the first-party API (confirm feature-specific requirements), and holds a compliance posture there.', 'Pin the full model ID using the anthropic. prefix format. Partner retirement dates differ from Anthropic\'s schedule. Confirm at publish time.'],
        ['Claude on Amazon Bedrock (legacy)', 'AWS identity and billing, InvokeModel/Converse APIs with ARN-versioned model identifiers.', 'The customer is on an existing Bedrock integration using InvokeModel or Converse and has not migrated to the Messages API.', 'Pin via ARN-versioned model identifiers per Bedrock\'s versioning controls.'],
        ['Google Vertex AI', 'Google Cloud identity, Identity and Access Management (IAM), and billing, with regional or global endpoints for residency.', 'The customer is on Google Cloud and holds a compliance posture there.', 'Pin the full model ID before rollout using Vertex\'s model ID format. Partner retirement dates differ from Anthropic\'s schedule.'],
        ['Third-party platform', 'The wrapping product\'s identity and billing model. Note: Claude in Microsoft Foundry offers two hosting forms: Hosted on Azure (currently Opus 4.8, Sonnet 5, and Haiku 4.5; inference end-to-end on Azure) and Hosted on Anthropic (all other Foundry Claude models). Confirm residency and compliance terms with Microsoft before selecting this path for a regulated customer.', 'The customer already runs the platform that embeds Claude.', 'Pin per the platform\'s versioning controls.'],
     ]},
    {m:'M5', titulo:'Cross-platform comparison',
     cols:['Dimension', 'How it differs by platform', 'How to measure it', 'Where each platform wins'],
     filas:[
        ['Latency', 'A platform in the customer\'s region shortens the round trip, while the first-party API may reach new features first.', 'From the customer\'s actual region against their actual payload.', 'An in-region cloud platform wins on round-trip latency, while the first-party API is advantaged on earliest feature access.'],
        ['Compliance', 'Data residency, certifications, and audit controls are determined by the deployment platform.', 'Against the customer\'s existing certification and residency requirements during scoping.', 'The cloud platform the customer has already certified wins, because it needs no re-certification.'],
        ['Cost', 'Token price, data egress, platform fees, and integration effort all vary.', 'Total cost per call per platform, including egress and integration, rather than token price alone.', 'The platform with the lowest total cost for the actual workload wins, which is not always the cheapest token.'],
     ]},
    {m:'M5', titulo:'Trust boundaries in an integration',
     cols:['Component', 'What it contributes', 'The trust boundary at its seam', 'The control that enforces it'],
     filas:[
        ['First-party API', 'Orchestrates the workflow and holds the entry point.', 'The request entering the app from outside.', 'Input validation and the identity the call runs under.'],
        ['Claude Code task', 'Runs the agentic work and may fetch external content.', 'Content it fetched, which is untrusted downstream.', 'Treat fetched content as data at the next seam.'],
        ['MCP server', 'Reaches a customer system to read or act.', 'The system access it holds on the app\'s behalf.', 'Scope the server to least privilege and log the access.'],
     ]},
];
