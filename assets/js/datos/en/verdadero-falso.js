/* True / false bank
   Extracted from tarjetas-interactivas.html · Anthropic Certification */

"use strict";

const trueFalseData = [
    {m:'M1', s:'Tokens map roughly to words and the ratio is the same across all Claude models.', v:false,
     e:'The average characters per token depends on the model’s tokenizer and differs between generations. Treat any rule of thumb as model-dependent and confirm it at build time.'},
    {m:'M1', s:'The context window holds the system prompt, the history, the tool definitions, the tool results and the model’s own output.', v:true,
     e:'It holds all of it at once. That is why a long session forces you to trim or summarize the history before each call.'},
    {m:'M1', s:'When the window fills during generation, the API silently truncates the oldest messages.', v:false,
     e:'Neither path silently trims your content: either you get a validation error up front, or the partial output comes back with stop_reason model_context_window_exceeded.'},
    {m:'M1', s:'The SDK and direct REST calls reach the same API and the same model.', v:true,
     e:'The SDK is a thin convenience wrapper that handles authentication, request building, retries and parsing. It does not change capabilities.'},
    {m:'M1', s:'A Message Batches API job can take up to 24 hours to complete.', v:true,
     e:'That is exactly the trade-off: high latency in exchange for a lower per-token cost, for offline work where nobody is waiting on each result.'},
    {m:'M1', s:'On the newest models, thinking content is included in the response by default.', v:false,
     e:'It is omitted by default. You have to request a summarized presentation when you need to show it.'},

    {m:'M2', s:'Claude executes the tools your schema defines.', v:false,
     e:'Claude decides which tool to call and with what arguments; your code runs it and returns the tool_result. Most bugs live between those two steps.'},
    {m:'M2', s:'A tool description should say when to use it and also when not to use it.', v:true,
     e:'The exclusion conditions are what stop Claude from confusing tools with similar descriptions.'},
    {m:'M2', s:'With structured outputs via json_schema you no longer need to check stop_reason.', v:false,
     e:'A refusal returns stop_reason refusal and a truncation returns stop_reason max_tokens. A guaranteed schema is not a guaranteed success.'},
    {m:'M2', s:'Prompt caching supports up to four cache breakpoints per request.', v:true,
     e:'You mark them with cache_control of type ephemeral on the last block you want cached. The best candidates are the prefixes that stay stable between turns.'},
    {m:'M2', s:'Batch results come back in the same order in which you submitted the requests.', v:false,
     e:'They come back in arbitrary order. Use each request’s custom_id field to match them to their inputs.'},
    {m:'M2', s:'A PDF is sent in a block of type "image", just like an image.', v:false,
     e:'The block type is "document". The source structure follows the same pattern (Base64, URL or file_id) and supports optional title and context.'},
    {m:'M2', s:'When you consume a stream, tool_use blocks arrive complete in a single event.', v:false,
     e:'They accumulate across multiple delta events. Consuming the stream without accounting for that produces partial tool inputs and silent downstream failures.'},
    {m:'M2', s:'Enlarging the context window is the right fix when a task does not fit in a single window.', v:false,
     e:'The fix is to decompose the task and pass only the relevant context to each subagent, which returns a summary to the parent.'},
    {m:'M2', s:'The Claude Agent SDK and the Anthropic SDK are the same product.', v:false,
     e:'The Agent SDK runs the agent loop that powers Claude Code. The Anthropic SDK is a thin convenience wrapper over the API and runs no loop at all.'},

    {m:'M3', s:'A deny rule can be overridden by a more specific allow rule.', v:false,
     e:'A deny rule always wins, whatever mode is in effect. The enterprise-level one also cannot be removed by any developer.'},
    {m:'M3', s:'Hooks run deterministically on the configured event, whatever the model decides.', v:true,
     e:'That is the difference between a guardrail and a convention. A PreToolUse hook can exit with code 2 to block the call before it runs.'},
    {m:'M3', s:'Subagents automatically inherit the parent session’s skills.', v:false,
     e:'They start clean. A skill the parent was using must be listed explicitly for the subagent, in every runtime that supports subagents.'},
    {m:'M3', s:'CLAUDE.md applies to all your Claude Code sessions on the machine.', v:false,
     e:'It only affects sessions working in that directory. Whatever must follow you everywhere goes in ~/.claude/settings.json.'},
    {m:'M3', s:'settings.local.json is automatically ignored by git.', v:true,
     e:'It is the place for personal overrides on one project. The project’s settings.json is committed and applies to the whole team.'},
    {m:'M3', s:'Allowlist mode prompts for confirmation when a tool is not permitted.', v:false,
     e:'It auto-denies anything not on the list; nothing queues for confirmation. It is built for CI and locked-down scripts, not for reducing interactive friction.'},
    {m:'M3', s:'Every connected MCP server adds its tool definitions to the context budget.', v:true,
     e:'That is why Claude Code defers them by default, and why you should connect only the servers a given task needs.'},
    {m:'M3', s:'A rules file and CLAUDE.md load the same way: always, on every session.', v:false,
     e:'CLAUDE.md loads unconditionally; a rules file activates only when you work in the directory it watches.'},

    {m:'M4', s:'A green unit test on every component guarantees the whole flow works.', v:false,
     e:'The unit test says nothing about how the pieces fit together. Silent failures live at the seam, which is what the integration test exercises.'},
    {m:'M4', s:'A model judge is reliable as soon as it returns consistent scores.', v:false,
     e:'It is only reliable after you calibrate it against human-labeled cases and measure agreement. An uncalibrated judge produces a number that looks rigorous and adds no value.'},
    {m:'M4', s:'A large eval set with somewhat noisier grading usually reveals more than a small hand-graded one.', v:true,
     e:'Coverage matters more than perfection: edge cases are found by volume.'},
    {m:'M4', s:'It is better to change prompt, examples and model at once to move faster.', v:false,
     e:'If the score moves you do not learn what caused it. Move one lever, re-run and read the per-case breakdown.'},
    {m:'M4', s:'A rate limit is a retriable error and deserves exponential backoff.', v:true,
     e:'And the backoff honors the retry-after value when the response carries one. A terminal error, such as a malformed request, should fail fast.'},
    {m:'M4', s:'Wrapping untrusted content in XML tags is a hard boundary against prompt injection.', v:false,
     e:'It is a soft boundary: the content can imitate your delimiters or argue it is an exception. The reliable boundary is what the agent is allowed to do.'},
    {m:'M4', s:'No agent that reads untrusted content is completely immune to prompt injection.', v:true,
     e:'Anthropic is explicit about this: model training and classifiers raise the bar, but they are probabilistic defenses. That is why the application also defends the action boundary.'},
    {m:'M4', s:'Streaming reduces the token cost of a response.', v:false,
     e:'Streaming changes how latency is perceived by returning the first token as soon as it is ready; the token cost is identical. The cost levers are the model, the context size, the number of tool calls, batching and caching.'},
    {m:'M4', s:'Spreading a task across parallel subagents always improves the result.', v:false,
     e:'It multiplies the token cost (≈15× in the reported case) and only buys something when the task decomposes into independent parts. For tightly coupled work, a single agent with good context wins.'},

    {m:'M5', s:'A model alias can resolve to different versions depending on the platform.', v:true,
     e:'An alias resolves to a recommended version that updates over time and can differ by platform. Pinning the full id is what prevents silent changes.'},
    {m:'M5', s:'Trust at a component boundary is inherited from the component that sent the data.', v:false,
     e:'It has to be established explicitly. Content fetched by one component is untrusted when it reaches the next, and is treated as data, not as instructions.'},
    {m:'M5', s:'An accelerator should include an eval that proves it still works in a new context.', v:true,
     e:'Together with the documented parameters and the written assumptions, that is what lets the next engagement configure it instead of rebuilding it.'},
    {m:'M5', s:'Working code is enough for a contribution to be ready for review.', v:false,
     e:'The maintainer needs to be able to verify it: focused code, a runnable example, a test that demonstrates the behavior, environment assumptions and confirmed rights.'},
    {m:'M5', s:'Latency measured from your development machine is a good proxy for production latency.', v:false,
     e:'You measure it from the client’s region and what matters is P95. And a data-residency constraint selects the platform before any optimization does.'}
];
