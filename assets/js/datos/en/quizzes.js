/* Multiple choice bank
   Extracted from tarjetas-interactivas.html · Anthropic Certification */

"use strict";

const quizzesData = [
    // ---- M1 ----
    {m:'M1', q:'temperature = 0 guarantees identical outputs across calls.',
     options:['True','False'], correct:1,
     explanation:'False. A low temperature concentrates probability and makes the output more repeatable, but it does not guarantee identical text. It is a concentrated distribution, not determinism.'},
    {m:'M1', q:'A colleague wants to tune temperature, top_p and top_k on a recent Claude model. What happens?',
     options:['It works normally','It returns a 400 error: new models do not accept non-default sampling parameters','They are silently ignored and the defaults are used','It reduces the available context window'], correct:1,
     explanation:'A 400 error. On those models, behavior is steered through prompting. Confirm parameter support in the API reference at build time.'},
    {m:'M1', q:'Your request fits in the window, but generation hits the ceiling halfway through. What does the API return?',
     options:['A validation error before generating','The output generated so far with stop_reason model_context_window_exceeded','It silently truncates the oldest messages and continues','It expands the window automatically for that call'], correct:1,
     explanation:'The up-front validation error belongs to the other case: an input that is already oversized. Here you get the partial output with model_context_window_exceeded. Neither path silently trims your content.'},
    {m:'M1', q:'When is the Message Batches API the right fit?',
     options:['When the user is waiting for the response on screen','When volume is high, nobody waits for each result and cost matters more than latency','Only for the smallest models','When you need concurrency without blocking the thread'], correct:1,
     explanation:'High-volume offline work, up to 24 h of acceptable latency and a lower per-token cost. Non-blocking concurrency is the async client’s case, which is a different pattern.'},
    {m:'M1', q:'Which statement correctly separates model choice from thinking mode?',
     options:['They are the same setting expressed two ways','Extended thinking is a different model in the family','Model choice selects which member runs; thinking is a per-request setting any compatible model supports','Thinking mode is set at the account level'], correct:2,
     explanation:'They are two independent controls that compose: a capable model without thinking is fast and direct; a smaller model with thinking spends more tokens reasoning.'},
    {m:'M1', q:'A short, well-specified classification already works zero-shot. What do you gain by adding three examples?',
     options:['A substantial accuracy improvement','Mostly spent tokens and context budget with no clear gain','The model retrains on those examples','You enable structured output'], correct:1,
     explanation:'Examples are not training data: they live in the prompt and cost tokens on every call. Add them when the description fails to pin down the structure, not by default.'},
    {m:'M1', q:'How is thinking depth calibrated today?',
     options:['With budget_tokens, setting the exact budget','With an effort setting; budget_tokens is deprecated and returns 400 on new generations','By raising the temperature','It is not configurable'], correct:1,
     explanation:'The current mode is adaptive thinking: the model decides when and how much to think, and you tune the depth with the effort setting.'},

    // ---- M2 ----
    {m:'M2', q:'A zero-shot classifier returns the right task but invents the structure. Which technique is missing?',
     options:['Structured outputs alone','Few-shot examples showing the exact pattern','An API key with more permissions','Raising max_tokens'], correct:1,
     explanation:'Right task but invented structure is the classic symptom of missing examples. One or two input-output pairs usually fix it faster than another paragraph of instructions.'},
    {m:'M2', q:'You use output_config.format with json_schema. What is NOT guaranteed?',
     options:['That the JSON is syntactically valid','That the JSON matches your schema','That the response can always be parsed: a refusal or a truncation still breaks the output','That the first call compiles the grammar'], correct:2,
     explanation:'Two cases remain: stop_reason refusal (the model declines) and stop_reason max_tokens (it stops mid-structure). Your code still checks stop_reason instead of assuming every response parses.'},
    {m:'M2', q:'Your agent loops forever. What is the correct control?',
     options:['Switch to a more capable model','Set a maximum iteration limit and clear exit conditions','Turn off extended thinking','Enlarge the context window'], correct:1,
     explanation:'An agent loop needs explicit termination conditions: a final answer, no useful tools, an unrecoverable error or a turn cap.'},
    {m:'M2', q:'With extended thinking and tool use, what do you do with the thinking blocks you receive?',
     options:['Summarize them to save context','Drop them: they already did their job','Return them to the API with no modification','Convert them into a text block'], correct:2,
     explanation:'Editing, summarizing or dropping them breaks the signature and causes a mismatch error. They go back exactly as they came.'},
    {m:'M2', q:'What is the invariant of the tool-use loop?',
     options:['Every tool_use must be answered with a tool_result of identical id in the immediately following user turn','tool_results can be grouped at the end of the conversation','Claude executes the tool and returns the result to you','The id only has to match when there is a single tool'], correct:0,
     explanation:'If the tool_result is missing or the id does not match exactly, the API returns a validation error. Most bugs live between Claude emitting the block and you running the tool.'},
    {m:'M2', q:'Your session is approaching the context ceiling but you want to keep working on the same feature without losing what you learned. Which strategy?',
     options:['Pruning','Compaction','Clearing','Switch to a model with a larger window'], correct:1,
     explanation:'Compaction summarizes the history while preserving the key information. Pruning discards an unproductive stretch, clearing starts from scratch, and enlarging the window is not the fix when the task does not fit.'},
    {m:'M2', q:'What must a compaction summarizer prompt explicitly ask for?',
     options:['"Summarize the conversation so far"','Preserving modified file paths, decisions made and errors hit with their resolution','Keeping the literal text of every turn','Only the last user message'], correct:1,
     explanation:'An underspecified summarizer discards the state that is critical to the task, and that loss is one of the most common causes of agent failures across sessions.'},
    {m:'M2', q:'How do you enable prompt caching?',
     options:['With a request-level cache: true parameter','By marking a breakpoint with cache_control of type ephemeral on the last block to cache (up to four breakpoints)','By reusing the same message_id','It is automatic on every model'], correct:1,
     explanation:'The best candidates are the parts that rarely change between turns: a long system prompt, a large set of tool definitions or a reference document.'},
    {m:'M2', q:'You need to know whether a request exceeds the window before sending it. What do you use?',
     options:['The count_tokens endpoint, which returns the count without running inference','A call with max_tokens=1','The usage field of the previous response','There is no way to know up front'], correct:0,
     explanation:'count_tokens accepts the same body as a messages call. Use it in development against real tool outputs, and in production to block requests that would exceed the window.'},
    {m:'M2', q:'You chunk a 5,000-entry list and loop over the sync API. Is that batching?',
     options:['Yes, that is exactly it','No: it produces the same number of calls and hits the same rate limits','Yes, as long as the chunks are 100 items','Only if you use the async client'], correct:1,
     explanation:'The Message Batches API is a different submission model, not a smaller batch size: a single call with up to 100,000 requests or 256 MB, a batch_id and polling until it completes.'},
    {m:'M2', q:'Your batch results come back out of order. How do you match them to the inputs?',
     options:['By position index in the array','By the custom_id field included in each request','By each result’s timestamp','By resubmitting the batch until they come out in order'], correct:1,
     explanation:'Results come back in arbitrary order, not in submission order. custom_id is the only reliable link.'},
    {m:'M2', q:'The same 200-page PDF is used across hundreds of requests. What is the correct pattern?',
     options:['Send it as Base64 on every request','Upload it to the Files API once and reference the file_id','Paste its text into the system prompt','Chunk it and send one chunk per request'], correct:1,
     explanation:'The Files API removes the redundant uploads. Combined with the Batches API and structured outputs it is the textbook pattern for high-volume offline pipelines.'},

    // ---- M3 ----
    {m:'M3', q:'Where should configuration you want loaded on every session of a project live?',
     options:['In the user’s global settings.json','In a CLAUDE.md file at the project root','On a remote MCP server','In shell environment variables'], correct:1,
     explanation:'CLAUDE.md content is prepended to the context window at the start of every session, and affects only sessions in that directory.'},
    {m:'M3', q:'What is CLAUDE.md’s main failure mode?',
     options:['It does not support Markdown','Size: beyond ~200-300 lines it dilutes the critical rules','It reloads on every tool call','It applies globally without you meaning it to'], correct:1,
     explanation:'A bigger file eats more window and makes each instruction a smaller fraction of what is loaded. Keep the behavior-changing constraints there and move the rest into skills.'},
    {m:'M3', q:'You need to deterministically prevent an edit to a production configuration path. What do you use?',
     options:['A clear instruction in CLAUDE.md','A PreToolUse hook that exits with code 2 and writes the reason to stderr','A comment in the protected file','Switching to a more capable model'], correct:1,
     explanation:'That is the difference between a guardrail and a convention: the hook runs on every relevant call, whatever the model decides.'},
    {m:'M3', q:'You delegated a task to the Explore subagent and it ignored a rule in your CLAUDE.md. Why?',
     options:['The subagent failed','Explore and Plan skip CLAUDE.md and git state to keep research fast','CLAUDE.md only applies to Bash commands','The rule was badly written'], correct:1,
     explanation:'The general-purpose subagent does load both. For tasks where your constraints must be respected, use that one or a custom subagent that explicitly loads what it needs.'},
    {m:'M3', q:'You define a custom subagent in .claude/agents that needs a specific skill. What must you do?',
     options:['Nothing: it inherits the parent session’s skills','List that skill explicitly in the agent’s frontmatter','Install the skill globally','Invoke it with a slash command inside the prompt'], correct:1,
     explanation:'Subagents start clean and do not inherit skills. Built-in agents have no preloaded skills either.'},
    {m:'M3', q:'A developer turns on bypassPermissions to reduce friction. What is the specific risk compared to the other modes?',
     options:['Only that it asks less often','Besides silencing confirmations, it skips the protected path guard','It disables configured hooks','It prevents reading files outside the project'], correct:1,
     explanation:'Only catastrophic deletions such as rm -rf / trigger a last-resort prompt. It is a mode for a disposable container or VM, never for a workstation against live code.'},
    {m:'M3', q:'A project-level allow rule contradicts an enterprise-level deny rule. Which applies?',
     options:['The project one, being more specific','The enterprise deny rule: a deny always wins and cannot be overridden','Whichever loaded first','Neither: the session does not start'], correct:1,
     explanation:'An enterprise-level deny rule (managed-settings.json) is the most durable governance control: it applies even when a bypass mode is set.'},
    {m:'M3', q:'Which file do you use for your personal preferences on a project without pushing them to the repository?',
     options:['~/.claude/settings.json','.claude/settings.json','.claude/settings.local.json','managed-settings.json'], correct:2,
     explanation:'settings.local.json holds personal overrides for one project and git ignores it automatically. The project’s settings.json is committed and applies to the whole team.'},
    {m:'M3', q:'How does Claude Code treat the tool definitions of connected MCP servers by default?',
     options:['It loads them all up front on every session','It defers them and uses a search step to load only the ones relevant to the task','It loads them only if the server is local','It compresses them before loading'], correct:1,
     explanation:'There is also an opt-in mode that loads them up front when they fit in roughly 10% of the window. Even so, connecting only the servers you need keeps each request lean.'},
    {m:'M3', q:'What is the practical difference between the stdio and HTTP transports in MCP?',
     options:['stdio is faster but less secure','stdio runs the server as a local subprocess; HTTP connects to a remote server over the network','stdio only supports resources, not tools','HTTP requires the server to be on the same machine'], correct:1,
     explanation:'The transport choice determines where the server can run and who can connect to it.'},
    {m:'M3', q:'What does a Claude Code plugin bundle?',
     options:['Skills only','Skills, hooks, subagents and MCP server configurations in one versioned, installable unit','Permission settings only','The team’s session history'], correct:1,
     explanation:'It is distributed through a marketplace and replaces a page of manual steps with a versioned, auditable install. Enterprise admins can roll it out across the organization.'},

    // ---- M4 ----
    {m:'M4', q:'Your eval passed but an edge case broke in production. What went wrong?',
     options:['The model changed version automatically','Insufficient coverage: the set did not exercise that variant','The judge’s rubric was too strict','The grader was code-based instead of a judge'], correct:1,
     explanation:'Coverage is what catches edge cases, and coverage comes from volume. Twenty cases with irregular inputs catch breakages that three chosen cases never exercise.'},
    {m:'M4', q:'The feature must return three capitals as a JSON array and one run returns them in a different order. Which grader fits?',
     options:['Exact string match','Code-based grading that parses the JSON and checks membership','LLM-as-judge with a rubric','None: the answer is wrong'], correct:1,
     explanation:'Exact match scores zero because the characters do not line up, even though the answer is correct. The method follows from the shape of the output.'},
    {m:'M4', q:'Your model judge almost always returns a 6. What is the rubric missing?',
     options:['A more capable model','Asking it for strengths, weaknesses and reasoning alongside the score','Lowering the judge’s temperature','Reducing the number of cases'], correct:1,
     explanation:'Without reasoning anchored to something specific, models drift to a safe middling number. Asking for the reasoning first is what anchors the score.'},
    {m:'M4', q:'Why is a model judge a bad inner loop on every commit?',
     options:['Because it is less accurate than exact match','Because it is a second model call per case: 1,000 cases are 1,000 extra calls per run','Because it does not support structured outputs','Because it needs human labels on every run'], correct:1,
     explanation:'Many teams grade format and structure with code on every commit and reserve the judge for a slower, scheduled quality pass.'},
    {m:'M4', q:'At which testing level do most silent production failures hide?',
     options:['Unit','Functional','Integration','End-to-end'], correct:2,
     explanation:'The integration test exercises the seam where two components hand off the work. Each side can pass its own tests while the handoff between them is broken.'},
    {m:'M4', q:'An eval fails and you do not know at which step. What tells you?',
     options:['An end-to-end test','A trace recording the prompt, tool calls, intermediate outputs and timings','Increasing the number of cases','Switching to the most capable model'], correct:1,
     explanation:'The trace turns "the case failed" into "step four: the parser threw a KeyError on a field the model did not return". That is the difference between five minutes and a day.'},
    {m:'M4', q:'A tool call fails. What is the first distinction you make?',
     options:['Whether the model is capable or not','Whether the error is retriable or terminal','Whether a user is waiting','Whether the prompt was zero-shot'], correct:1,
     explanation:'Retriable (rate limit, overload) → exponential backoff. Terminal (malformed request) → it will fail the same way, so fail fast instead of burning the retry budget.'},
    {m:'M4', q:'Your orchestrator-worker setup tripled the bill and the answers barely improved. Why?',
     options:['The subagents used too large a model','The task did not decompose into independent parts: each step depended on the previous one','Prompt caching was missing','The orchestrator did not have thinking enabled'], correct:1,
     explanation:'Each subagent burns its own tokens against its own context, roughly 15× in the case Anthropic reported. That multiplier only buys something when the work can genuinely run in parallel.'},
    {m:'M4', q:'What is the reliable defense against prompt injection?',
     options:['Writing the system prompt more carefully','Trusting only internal users','Treating fetched content as data and enforcing the boundary on what the agent can do','Turning off streaming'], correct:2,
     explanation:'Delimiters and model training raise the bar but are probabilistic. The hostile example is harmless if the agent has no tool able to perform that action.'},
    {m:'M4', q:'Which of these is NOT a cost-reduction lever?',
     options:['Choosing a smaller model for the task when the eval allows it','Trimming context and unnecessary tool output','Enabling streaming so the response feels instant','Caching the system prompt and stable tool schemas'], correct:2,
     explanation:'Streaming changes perceived latency by returning the first token sooner, but it does not reduce the tokens consumed or the cost of the call.'},
    {m:'M4', q:'A jailbreak and a prompt injection differ in that…',
     options:['They are the same thing under different names','The jailbreak tries to make the model ignore its safety constraints; the injection tries to hijack your application’s instructions','A jailbreak only happens in fetched content','Injection only affects models without thinking'], correct:1,
     explanation:'Different targets, same shape of defense: validate and constrain what reaches the model, and limit what it is allowed to do as a result.'},
    {m:'M4', q:'You apply least privilege to your production agent. Which detail is most often overlooked?',
     options:['Rotating the secret every 90 days','That anything able to modify the auth configuration can act with that identity','Encrypting the logs','Using HTTPS on every call'], correct:1,
     explanation:'Protecting the auth configuration matters as much as protecting the secret itself.'},

    // ---- M5 ----
    {m:'M5', q:'You are deploying to production. Model alias or full pinned id?',
     options:['Alias, for flexibility as improvements land','Full pinned id: the alias resolves to a version that updates and can differ by platform','It makes no difference','Alias in production and pinned in development'], correct:1,
     explanation:'Pinning is what stops an upstream model change from becoming a silent change in production. Also keep a retained prior version as a rollback target.'},
    {m:'M5', q:'What separates an accelerator from a solution that merely works?',
     options:['That it is written in Python','That the next engagement configures it instead of rebuilding it: documented parameters, written assumptions and an included eval','That it was published in a public repository','That it uses the most capable model'], correct:1,
     explanation:'The common mistake is the template shipped fast with hardcoded paths, configuration in the code and no documentation.'},
    {m:'M5', q:'Your PR has sat unreviewed for three weeks even though the code runs daily. What is missing?',
     options:['Tagging more reviewers','What the maintainer needs to verify it: a runnable example, a test and declared environment assumptions','Rewriting it in another language','Opening an issue instead of a PR'], correct:1,
     explanation:'A contribution a reviewer cannot verify goes to the back of the queue. A focused PR with a test and an example gets reviewed quickly because nothing is left to reverse-engineer.'},
    {m:'M5', q:'"Extract 5 risk clause types at 80% confidence" is a requirement that is…',
     options:['Infrastructure','Functional','Compliance','Not a requirement, it is a success criterion'], correct:1,
     explanation:'The functional one says what the system does. "Run on AWS for compliance" or "respond in under 30 s" are infrastructure: how and where it runs.'},
    {m:'M5', q:'You must meet EU-only data residency and you measured 180 ms of latency on your laptop. What do you do first?',
     options:['Optimize the parser to shave off those 180 ms','Re-measure latency from the client’s region and pick the platform whose region satisfies residency','Add a caching layer','Switch to a faster model'], correct:1,
     explanation:'The compliance constraint selects the platform before any optimization, and the relevant latency is measured from the client’s region, not from your machine.'},
    {m:'M5', q:'Three components pass their own tests and the application turns out to be insecure. What was skipped?',
     options:['A unit test of the central component','Marking the seam between components as a trust boundary','Encrypting internal traffic','Pinning the model version'], correct:1,
     explanation:'Content fetched by one component is untrusted when it reaches the next. Trust at a boundary must be established explicitly; it is not inherited from the component that sent the data.'},
    {m:'M5', q:'You find a seam you cannot secure before the launch date. What is the right call?',
     options:['Ship and log the risk','Send it to a human owner instead of shipping it to production','Wrap the content in XML tags and ship','Reduce the model’s scope'], correct:1,
     explanation:'When a seam cannot be secured, it goes to a human owner instead of shipping to production.'},
    {m:'M5', q:'What is true about Claude deployment platforms?',
     options:['The same model behaves identically on all of them','The same model can differ by platform in identity, data residency, latency and cost','Only the first-party API supports thinking','Platform choice does not affect versioning'], correct:1,
     explanation:'That is why platform choice is an architecture decision defended with documented requirements, not a deployment detail.'}
];
